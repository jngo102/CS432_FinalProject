/// Contains material data
class Material {
    constructor(texture, ambient, diffuse, specular, alpha) {
        this.texture = texture;
        this.ambient = ambient;
        this.diffuse = diffuse;
        this.specular = specular;
        this.alpha = alpha;
    }
}